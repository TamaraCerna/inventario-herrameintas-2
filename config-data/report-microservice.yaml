server:
  port: 0   # puerto dinámico (recomendado con Eureka y K8s)

spring:
  application:
    name: report-service

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info
